<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    
    <Properties>
        <property name="basePath">./logs</property>
        <property name="logPattern">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] [%X{requestId}] %-5level %logger{36} - %msg%n</property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${logPattern}" />
        </Console>

        <!-- Rolling File Appender -->
        <RollingFile name="File" 
                     fileName="${basePath}/automation.log" 
                     filePattern="${basePath}/automation-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="${logPattern}" />
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB" />
                <TimeBasedTriggeringPolicy />
            </Policies>
            <DefaultRolloverStrategy max="10" fileIndex="min" />
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Logger for application-specific logs -->
        <Logger name="com.mycompany.myapp" level="DEBUG" additivity="false">
            <AppenderRef ref="File" />
            <AppenderRef ref="Console" />
        </Logger>

        <!-- Root Logger -->
        <Root level="INFO">
            <AppenderRef ref="File" />
            <AppenderRef ref="Console" />
        </Root>
    </Loggers>
</Configuration>
